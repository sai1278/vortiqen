# ==============================================================================
# Vortiqen Production Environment Configuration Template
# All production secrets and configuration keys are documented below.
# DO NOT COMMIT REAL SECRETS TO REPOSITORY.
# ==============================================================================

# Database Persistence
POSTGRES_DB=
POSTGRES_USER=
POSTGRES_PASSWORD=
POSTGRES_PORT=
DATABASE_URL=

# Application Environment
ENVIRONMENT=
APP_NAME=
ALLOWED_ORIGINS=
ALLOW_LOCAL_PERSISTENCE_IN_PRODUCTION=

# Rate Limiting & Proxy Configuration
RATE_LIMIT_WINDOW_SECONDS=
RATE_LIMIT_MAX_REQUESTS=
TRUSTED_PROXY_COUNT=

# Lead Notifications - Webhook / Slack / Discord
NOTIFICATION_WEBHOOK_URL=
NOTIFICATION_WEBHOOK_TIMEOUT_SECONDS=

# Lead Notifications - Baserow Table API
BASEROW_TABLE_URL=
BASEROW_API_TOKEN=
BASEROW_TIMEOUT_SECONDS=

# Lead Notifications - SMTP Email
SMTP_HOST=
SMTP_PORT=
SMTP_USER=
SMTP_PASSWORD=
SMTP_STARTTLS=
SENDER_EMAIL=
TEAM_INBOX=

# Frontend Public Configuration (baked into client bundle during build)
VITE_API_BASE_URL=
